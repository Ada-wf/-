# 求职城市网 — 开发步骤

> **版本**: v1.0
> **创建日期**: 2026-07-06
> **适用对象**: AI 辅助开发（Claude Code 搭子）

---

## 如何使用本文档

每个步骤包含：
- 🎯 **目标**: 这一步要达成什么
- 📁 **文件清单**: 需要创建/修改的文件
- 🔧 **操作指令**: 可直接交给 AI 执行的具体任务
- ✅ **验收条件**: 怎么判断这一步完成了

**建议**: 按顺序执行，每一步完成并验收后再进入下一步。`🔴`=必须按顺序，`🟡`=可以并行。

---

## 第一阶段：项目基础（第 1 天）

---

### 步骤 0: 环境准备

**🎯 目标**: 确认开发环境就绪

```bash
# 检查 Node.js 版本 (需要 >= 20)
node -v

# 检查包管理器
npm -v
# 或
yarn -v
```

**✅ 验收**: Node.js >= 20，npm/yarn 可用

---

### 步骤 1: 🔴 创建 Next.js 项目

**🎯 目标**: 用最新 Next.js 15+ 脚手架创建项目

**📁 文件清单**:
```
D:\求职城市网\
├── package.json          (自动生成)
├── tsconfig.json         (自动生成)
├── next.config.ts        (自动生成)
├── tailwind.config.ts    (自动生成)
├── postcss.config.mjs    (自动生成)
├── src/app/
│   ├── layout.tsx        (自动生成)
│   ├── page.tsx          (自动生成)
│   └── globals.css       (自动生成)
└── public/               (自动生成)
```

**🔧 操作指令**:
```bash
cd D:\求职城市网
git init
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack
```

**✅ 验收**: `npm run dev` → 浏览器 `localhost:3000` 看到 Next.js 默认首页

---

### 步骤 2: 🔴 安装核心依赖 + 配置文件

**🎯 目标**: 安装所有 Phase 1 需要的依赖包

**📁 文件清单**:
```
├── package.json          (更新)
├── velite.config.ts      (新建)
└── src/
    ├── types/
    │   ├── career.ts     (新建)
    │   └── category.ts   (新建)
    └── lib/
        ├── careers.ts    (新建)
        └── categories.ts (新建)
```

**🔧 操作指令**:

安装生产依赖:
```bash
npm install velite lucide-react fuse.js gray-matter js-yaml zod
```

安装开发依赖:
```bash
npm install -D @types/node prettier
```

**创建 `velite.config.ts`**:
```typescript
import { defineConfig, s } from 'velite'

export default defineConfig({
  root: 'content/careers',
  collections: {
    careers: {
      name: 'Career',
      pattern: '**/*.md',
      schema: s.object({
        slug: s.string(),
        name: s.string(),
        nameEn: s.string().optional(),
        tagline: s.string(),
        categoryPath: s.object({
          discipline: s.object({ id: s.string(), name: s.string() }),
          school: s.object({ id: s.string(), name: s.string() }),
          major: s.object({ id: s.string(), name: s.string() }),
        }),
        conceptImage: s.object({
          url: s.string(),
          alt: s.string(),
          source: s.enum(['manual', 'ai-generated']),
          generatedAt: s.string().optional(),
        }),
        overview: s.string(),
        responsibilities: s.array(s.string()),
        typicalDay: s.string().optional(),
        suggestion: s.object({
          personalityFit: s.array(s.string()),
          skillsRequired: s.array(s.string()),
          summary: s.string(),
          notForYou: s.string().optional(),
        }),
        salaryRange: s.object({
          junior: s.string(),
          senior: s.string(),
          source: s.string(),
        }).optional(),
        educationRequired: s.enum(['高中', '大专', '本科', '硕士', '博士']).optional(),
        industryOutlook: s.enum(['growth', 'stable', 'declining', 'emerging']).optional(),
        certifications: s.array(s.string()).optional(),
        relatedCareers: s.array(s.string()).optional(),
        tags: s.array(s.string()),
        source: s.enum(['curated', 'ai-generated']),
        generatedAt: s.string().optional(),
        verifiedAt: s.string().optional(),
        metadata: s.object({
          createdAt: s.string(),
          updatedAt: s.string(),
          viewCount: s.number().optional(),
          feedbackScore: s.number().optional(),
        }),
      })
    }
  }
})
```

**创建 `src/types/career.ts`**:
```typescript
// 从 DEVELOP.md 第三章复制完整的 Career 接口定义
```

**创建 `src/types/category.ts`**:
```typescript
// 从 DEVELOP.md 第三章复制完整的分类接口定义
```

**创建 `src/lib/careers.ts`**:
```typescript
import { careers } from '@/.velite'

export function getAllCareers() {
  return careers
}

export function getCareerBySlug(slug: string) {
  return careers.find(c => c.slug === slug) ?? null
}

export function getCareersByDiscipline(disciplineId: string) {
  return careers.filter(c => c.categoryPath.discipline.id === disciplineId)
}

export function getRandomCareers(count: number, excludeSlugs: string[] = []) {
  const pool = careers.filter(c => !excludeSlugs.includes(c.slug))
  const shuffled = pool.sort(() => Math.random() - 0.5)
  return shuffled.slice(0, Math.min(count, pool.length))
}

export function getRelatedCareers(slug: string) {
  const career = getCareerBySlug(slug)
  if (!career?.relatedCareers) return []
  return career.relatedCareers
    .map(s => getCareerBySlug(s))
    .filter(Boolean)
}
```

**创建 `src/lib/categories.ts`**:
```typescript
import fs from 'fs'
import path from 'path'
import yaml from 'js-yaml'
import type { DisciplineCategory } from '@/types/category'

let cached: DisciplineCategory[] | null = null

function loadCategories(): DisciplineCategory[] {
  if (cached) return cached
  const filePath = path.join(process.cwd(), 'content/categories/tsinghua-categories.yaml')
  const raw = fs.readFileSync(filePath, 'utf-8')
  const data = yaml.load(raw) as { disciplines: DisciplineCategory[] }
  cached = data.disciplines
  return cached
}

export function getAllDisciplines() {
  return loadCategories()
}

export function getDisciplineById(id: string) {
  return loadCategories().find(d => d.id === id) ?? null
}

export function getSchoolsByDiscipline(disciplineId: string) {
  const d = getDisciplineById(disciplineId)
  return d?.schools ?? []
}

export function getMajorsBySchool(schoolId: string) {
  for (const d of loadCategories()) {
    const school = d.schools.find(s => s.id === schoolId)
    if (school) return school.majors
  }
  return []
}

export function getCareersByMajor(majorId: string) {
  for (const d of loadCategories()) {
    for (const s of d.schools) {
      const major = s.majors.find(m => m.id === majorId)
      if (major) return major.careerSlugs
    }
  }
  return []
}
```

**✅ 验收**: `npm run dev` 无报错；TypeScript 编译通过

---

### 步骤 3: 🔴 配置 Tailwind + 全局样式

**🎯 目标**: 建立设计规范中的色彩系统和基础样式

**📁 文件清单**:
```
├── tailwind.config.ts    (修改)
└── src/styles/
    └── globals.css       (修改/移动到 src/styles/)
```

**🔧 操作指令**:

修改 `tailwind.config.ts`，添加自定义颜色：
```typescript
import type { Config } from 'tailwindcss'

export default {
  content: ['./src/**/*.{ts,tsx}'],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        science: '#2563EB',
        engineering: '#DC2626',
        medicine: '#059669',
        management: '#7C3AED',
        humanities: '#D97706',
        'social-science': '#0891B2',
        law: '#B45309',
        arts: '#DB2777',
        economics: '#CA8A04',
        education: '#4F46E5',
        agriculture: '#65A30D',
        cross: '#9333EA',
      },
    },
  },
  plugins: [require('@tailwindcss/typography')],
} satisfies Config
```

修改 `src/styles/globals.css`，建立 CSS 变量（从 DEVELOP.md 第 8.1 节复制完整的亮色/暗色变量）。

**✅ 验收**: `npm run dev` → 页面使用新的颜色变量

---

### 步骤 4: 🔴 初始化 Git 仓库

**🎯 目标**: 建立版本控制 + CI 流水线

**📁 文件清单**:
```
├── .gitignore
├── .prettierrc
├── .github/workflows/ci.yml
└── scripts/
    └── validate-content.ts
```

**🔧 操作指令**:

创建 `.gitignore`:
```
node_modules/
.next/
.velite/
.env
.env.local
```

创建 `.prettierrc`:
```json
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "all"
}
```

创建 `.github/workflows/ci.yml`:
```yaml
name: CI
on: [push, pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npx prettier --check .
```

创建 `scripts/validate-content.ts`（按测试用例 TC-CONTENT-001/002 的校验逻辑编写）。

**✅ 验收**: `git status` 干净；`npx tsc --noEmit` 通过

---

## 第二阶段：数据层（第 2-4 天）

---

### 步骤 5: 🔴 编写分类 YAML 数据

**🎯 目标**: 完成全部 12 个学科门类的分类数据

**📁 文件清单**:
```
└── content/categories/
    └── tsinghua-categories.yaml   (新建，从 DEVELOP.md 第三章复制完整数据)
```

**🔧 操作指令**:
- 从 DEVELOP.md 第三章复制完整 YAML 内容
- 确认 12 门类、30+ 学院、90+ 专业、400+ careerSlugs 引用

**✅ 验收**:
```bash
npx tsx scripts/validate-content.ts  # 校验通过
```

---

### 步骤 6: 🔴 编写首批职业数据（25 个）

**🎯 目标**: 完成前 25 个核心职业的 Markdown 文件

**📁 文件清单**:
```
└── content/careers/
    ├── software-engineer.md     (新建)
    ├── frontend-engineer.md     (新建)
    ├── algorithm-engineer.md    (新建)
    ├── data-scientist.md        (新建)
    ├── product-manager.md       (新建)
    ├── doctor.md                (新建)
    ├── lawyer.md                (新建)
    ├── accountant.md            (新建)
    ├── architect.md             (新建)
    ├── graphic-designer.md      (新建)
    ├── civil-engineer.md        (新建)
    ├── mechanical-engineer.md   (新建)
    ├── journalist.md            (新建)
    ├── psychologist.md          (新建)
    ├── teacher.md               (新建)
    ├── investment-banker.md     (新建)
    ├── ai-engineer.md           (新建)
    ├── devops-engineer.md       (新建)
    ├── ux-designer.md           (新建)
    ├── diplomat.md              (新建)
    ├── agronomist.md            (新建)
    ├── economist.md             (新建)
    ├── animator.md              (新建)
    ├── cybersecurity-analyst.md (新建)
    └── registered-nurse.md      (新建)
```

**🔧 操作指令**:
- 对每个职业，参照 DEVELOP.md 第三章 `software-engineer.md` 示例格式编写
- 必须填写 `slug, name, nameEn, tagline, categoryPath, conceptImage, overview, responsibilities, suggestion, tags, source`
- 尽量填写新字段 `salaryRange, educationRequired, industryOutlook, relatedCareers`
- 每个职业 800-1200 字中文内容
- 可以用 Claude 辅助生成初稿，每批 5 个职业为一组

**✅ 验收**:
```bash
npx tsx scripts/validate-content.ts  # 全部 25 个文件通过
```

---

### 步骤 7: 🟡 生成概念图（前 25 张）

**🎯 目标**: 为已完成数据的职业生成概念图

> 可以与步骤 6 并行（写完一个职业就生成图）

**📁 文件清单**:
```
└── public/images/careers/
    ├── software-engineer.webp
    ├── frontend-engineer.webp
    └── ... (共 25 张)
```

**🔧 操作指令**:

创建 `scripts/generate-images.ts`:
```typescript
// 批量调用 DALL·E 3 + Cloudinary 上传
// 读取 content/careers/ 下无概念图的职业，逐个生成
// 使用 DEVELOP.md 6.4 节的 buildImagePrompt
```

```bash
npx tsx scripts/generate-images.ts
```

**✅ 验收**: 每个职业的概念图存在且风格一致（人工抽查 5 张）

---

## 第三阶段：页面开发（第 5-9 天）

---

### 步骤 8: 🔴 全局布局 (Layout)

**🎯 目标**: 创建 Header + Footer + 全局布局壳子

**📁 文件清单**:
```
└── src/
    ├── app/layout.tsx                    (修改)
    ├── app/page.tsx                      (修改，先放占位内容)
    └── components/layout/
        ├── Header.tsx                    (新建)
        └── Footer.tsx                    (新建)
```

**🔧 操作指令**:

修改 `src/app/layout.tsx`:
```typescript
import type { Metadata } from 'next'
import { Header } from '@/components/layout/Header'
import { Footer } from '@/components/layout/Footer'
import '@/styles/globals.css'

export const metadata: Metadata = {
  title: '求职城市网 — 探索你的职业城市',
  description: '基于清华大学学科体系，发现 400+ 职业的精彩',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh-CN" suppressHydrationWarning>
      <body className="min-h-screen flex flex-col bg-[var(--color-bg)] text-[var(--color-text)]">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  )
}
```

创建 `Header.tsx`:
```typescript
'use client'

import Link from 'next/link'
import { Search } from 'lucide-react'
// 包含: Logo → 搜索框 → 分类入口 → 暗色模式切换

export function Header() {
  return (
    <header className="sticky top-0 z-50 bg-[var(--color-surface)] border-b border-[var(--color-border)]">
      {/* ... DEVELOP.md 4.1 节的布局 */}
    </header>
  )
}
```

创建 `Footer.tsx`:
```typescript
export function Footer() {
  return (
    <footer className="bg-[var(--color-surface)] border-t border-[var(--color-border)] py-8">
      {/* © 2026 求职城市网 | 数据来源说明 | 联系我们 */}
    </footer>
  )
}
```

**✅ 验收**: 所有页面显示 Header 和 Footer；响应式正常

---

### 步骤 9: 🔴 首页 — 随机卡片网格

**🎯 目标**: 实现首页的完整功能（HeroBanner + 学科筛选 + 卡片网格 + 换一批）

**📁 文件清单**:
```
└── src/
    ├── app/page.tsx                     (修改)
    └── components/home/
        ├── HeroBanner.tsx               (新建)
        ├── CareerCardGrid.tsx           (新建)
        ├── CareerCard.tsx               (新建)
        └── CategoryFilter.tsx           (新建)
```

**🔧 操作指令**:

1. **创建 `CareerCard.tsx`**: 按 DEVELOP.md 4.2 节和 8.3 节设计
   ```typescript
   // Props: career: Career
   // 渲染: 概念图 + 名称 + tagline + 标签 + 薪资
   // CSS: hover 上浮动画 + 圆角阴影
   // 点击: router.push(`/career/${career.slug}`)
   ```

2. **创建 `CareerCardGrid.tsx`**:
   ```typescript
   // Props: careers: Career[], onRefresh: () => void
   // 渲染: 响应式网格 (grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-5)
   // 空状态: <EmptyState> 组件
   ```

3. **创建 `CategoryFilter.tsx`**:
   ```typescript
   // 横向滚动的 Tab 栏: "全部" + 12 门类
   // Props: selected: string | null, onSelect: (id: string | null) => void
   ```

4. **创建 `HeroBanner.tsx`**: 首页横幅（搜索框 + 标语）

5. **修改 `src/app/page.tsx`**:
   ```typescript
   // 客户端组件
   // 状态: careers, selectedDiscipline, loading
   // 逻辑: 随机获取 15 个职业 → 学科筛选 → 换一批重新随机
   ```

**✅ 验收**:
- 首页显示 10-15 张随机卡片
- 卡片 hover 动画和点击跳转正常
- 学科筛选过滤正常
- "换一批"更换卡片
- 移动端 1 列、平板 2-3 列、桌面 4-5 列

---

### 步骤 10: 🔴 分类总览页

**🎯 目标**: 实现 `/categories` 页面

**📁 文件清单**:
```
└── src/
    ├── app/categories/page.tsx         (新建)
    └── components/category/
        └── CategoryBreadcrumb.tsx      (新建)
```

**🔧 操作指令**:

```typescript
// src/app/categories/page.tsx
// 服务端组件 (SSG)
// 读取 getAllDisciplines()
// 渲染 12 个门类卡片 (按 DEVELOP.md 4.3 节布局)
// 每个卡片: 图标 + 名称 + 学院列表 + 职业数量
// 点击跳转 /categories/[discipline]
```

**✅ 验收**: 12 个门类卡片正确渲染；点击跳转正常

---

### 步骤 11: 🔴 思维导图导航页（桌面端 + 移动端）

**🎯 目标**: 实现交互式思维导图

**📁 文件清单**:
```
└── src/
    ├── app/categories/[discipline]/page.tsx  (新建)
    └── components/category/
        ├── MindMap.tsx                       (新建，G6 TreeGraph)
        └── CategoryTree.tsx                  (新建，移动端层级列表)
```

**🔧 操作指令**:

1. **创建 `MindMap.tsx`** (桌面端):
   ```typescript
   'use client'
   import G6, { TreeGraph } from '@antv/g6'
   import { useEffect, useRef } from 'react'

   // 接收 category 数据
   // 构建四级树: 学科 → 学院 → 专业 → 职业
   // 配置 G6 TreeGraph: 从左到右布局、节点颜色、点击交互
   // 点击职业节点 → router.push(`/career/${slug}`)
   // 展开/收起子节点
   // 搜索高亮
   ```

2. **创建 `CategoryTree.tsx`** (移动端):
   ```typescript
   'use client'
   // 使用 <details>/<summary> 或自定义展开状态
   // 渐进展开: 第一层显示学院 → 点击展开专业 → 点击展开职业
   // 职业点击跳转详情页
   // 面包屑显示当前位置
   ```

3. **创建 `src/app/categories/[discipline]/page.tsx`**:
   ```typescript
   // 读取分类数据
   // detect mobile / desktop → 渲染 MindMap 或 CategoryTree
   // 面包屑: 首页 > 学科分类 > {学科名}
   ```

**✅ 验收**:
- 桌面端: 思维导图四级节点正确，点击展开/收起，缩放拖拽正常
- 移动端: 渐进展开层级列表，点击可逐层展开
- 点击叶子节点跳转详情页
- 无 JS 报错

---

### 步骤 12: 🔴 职业详情页

**🎯 目标**: 实现完整的职业详情页

**📁 文件清单**:
```
└── src/
    ├── app/career/[slug]/page.tsx            (新建)
    └── components/career/
        ├── CareerHero.tsx                    (新建)
        ├── CareerDecisionCard.tsx            (新建)
        ├── CareerOverview.tsx                (新建)
        ├── CareerSuggestion.tsx              (新建)
        ├── CareerCompare.tsx                 (新建)
        └── CareerBreadcrumb.tsx              (新建)
```

**🔧 操作指令**:

1. **`src/app/career/[slug]/page.tsx`** (SSG):
   ```typescript
   // generateStaticParams: 返回所有 slug
   // generateMetadata: 动态 title/description
   // 读取 career 数据
   // 渲染: 面包屑 → CareerHero → CareerDecisionCard → CareerOverview → CareerSuggestion → CareerCompare
   // 404 处理: notFound()
   ```

2. **`CareerHero.tsx`**: 大型概念图 + 职业名 + 英文名 + tagline

3. **`CareerDecisionCard.tsx`**: 快速决策卡（4 格布局）
   - 💰 薪资 → salaryRange
   - 🎓 学历 → educationRequired
   - 📈 前景 → industryOutlook
   - 📜 证书 → certifications

4. **`CareerOverview.tsx`**: 工作概述 Markdown 渲染

5. **`CareerSuggestion.tsx`**: 两列布局
   - 左: personalityFit + summary
   - 右: skillsRequired + notForYou

6. **`CareerCompare.tsx`**: 相似职业对比表

**✅ 验收**:
- 所有字段正确渲染
- 缺失可选字段时不崩溃
- 快速决策卡正确显示
- 相似职业对比表正确
- 面包屑路径正确
- AI 生成内容显示免责声明

---

### 步骤 13: 🔴 站内搜索

**🎯 目标**: 实现搜索框 + 搜索结果页 + 搜索 API

**📁 文件清单**:
```
└── src/
    ├── app/search/page.tsx                (新建)
    ├── app/api/search/route.ts            (新建)
    ├── lib/search.ts                      (新建)
    └── components/search/
        ├── SearchBox.tsx                  (新建)
        ├── SearchResultCard.tsx           (新建)
        └── AIGeneratePanel.tsx            (新建，占位)
```

**🔧 操作指令**:

1. **`src/lib/search.ts`**:
   ```typescript
   import Fuse from 'fuse.js'
   import { getAllCareers } from './careers'

   // 初始化 Fuse 索引
   // searchCareers(query, options) → { results, suggestions, aiGenerateAvailable }
   // 搜索字段: name, tagline, tags, overview
   ```

2. **`src/app/api/search/route.ts`**:
   ```typescript
   // GET /api/search?q=xxx&limit=10
   // 校验参数 → 调用 searchCareers → 返回 JSON
   // 错误处理: 缺参数 400，速率限制 429
   ```

3. **`SearchBox.tsx`**: 输入框 + 防抖建议下拉

4. **`src/app/search/page.tsx`**: 搜索结果页
   - 有结果: 渲染 SearchResultCard 列表
   - 无结果: AIGeneratePanel（先放占位，Phase 2 实现）

**✅ 验收**:
- 搜索框输入防抖建议正常
- 搜索"软件"能找到多个结果
- 点击结果跳转详情页
- 搜索"xyz"显示未找到 + AI 生成入口
- API 直接调用返回正确 JSON

---

### 步骤 14: 🔴 部署上线

**🎯 目标**: 部署到 Vercel + 配置 CI/CD

**🔧 操作指令**:

```bash
# 1. 推送到 GitHub
git add .
git commit -m "feat: Phase 1 MVP"
git remote add origin https://github.com/xxx/qiuzhi-city.git
git push -u origin main

# 2. Vercel 导入 GitHub 仓库
# 访问 vercel.com → Import → 选择仓库 → Deploy

# 3. 配置域名 (可选)
# Vercel Dashboard → Settings → Domains
```

**✅ 验收**:
- 公网可访问
- CI 流水线绿灯 (lint + typecheck)
- Lighthouse 分数 > 90

---

## 第四阶段：AI 功能（第 10-13 天）

---

### 步骤 15: 🔴 Claude API 集成

**🎯 目标**: 实现 AI 文本生成核心

**📁 文件清单**:
```
├── .env.local                (新建，API keys)
└── src/lib/
    ├── ai-generate.ts        (新建)
    └── ai-review.ts          (新建)
```

**🔧 操作指令**:

创建 `.env.local`:
```
ANTHROPIC_API_KEY=sk-ant-xxx
TAVILY_API_KEY=tvly-xxx
OPENAI_API_KEY=sk-xxx
REPLICATE_API_TOKEN=r8_xxx
CLOUDINARY_CLOUD_NAME=xxx
CLOUDINARY_API_KEY=xxx
CLOUDINARY_API_SECRET=xxx
```

创建 `src/lib/ai-generate.ts`:
```typescript
// 实现 DEVELOP.md 第 6.2 节的 Prompt 模板
// 函数: generateCareerText(query, searchResults) → Career (不含图片)
// 函数: buildImagePrompt(careerName, nameEn, overview) → string
// 包含: 分类树嵌入 + Few-shot 示例 + 安全护栏
```

创建 `src/lib/ai-review.ts`:
```typescript
// 实现 DEVELOP.md 第 6.3 节的审核 Prompt
// 函数: reviewCareerContent(careerData, searchSources) → { passed, issues, severity }
```

**✅ 验收**:
```typescript
// 手动测试
import { generateCareerText } from '@/lib/ai-generate'
const result = await generateCareerText('碳中和管理师', mockSearchResults)
// result 符合 Career 接口，categoryPath 在分类树中存在
```

---

### 步骤 16: 🔴 Tavily + 图片生成 + Cloudinary 集成

**🎯 目标**: 实现完整的 AI 管线（搜索 → 文本 → 审核 → 图片 → CDN）

**📁 文件清单**:
```
└── src/lib/
    ├── search-web.ts          (新建，Tavily 封装)
    ├── image-generate.ts      (新建，DALL·E + Replicate)
    └── cloudinary.ts          (新建，上传)
```

**🔧 操作指令**:

1. **`src/lib/search-web.ts`**:
   ```typescript
   // 封装 Tavily Search API
   // searchWeb(query) → { url, title, content }[]
   ```

2. **`src/lib/image-generate.ts`**:
   ```typescript
   // 主: DALL·E 3 → 备: Replicate Flux
   // generateConceptImage(careerName, nameEn, overview) → imageUrl
   // 自动切换逻辑
   ```

3. **`src/lib/cloudinary.ts`**:
   ```typescript
   // uploadImage(imageUrl, slug) → cloudinaryUrl
   // 自动 WebP 转换 + 统一尺寸
   ```

**✅ 验收**:
```bash
# 手动测试整个管线
npx tsx scripts/test-ai-pipeline.ts  # 或写一个测试脚本
```

---

### 步骤 17: 🔴 SSE 流式 API + Vercel KV 存储

**🎯 目标**: 实现 `/api/ai-generate` 端点

**📁 文件清单**:
```
└── src/app/api/ai-generate/
    └── route.ts               (新建)
```

**🔧 操作指令**:

```typescript
// src/app/api/ai-generate/route.ts
export async function POST(request: Request) {
  // 1. 解析请求 → 校验参数
  // 2. 速率限制检查
  // 3. 检查 KV 缓存 (重复查询)
  // 4. 返回 SSE ReadableStream
  //    事件序列: searching → found_web_data → generating_text
  //            → reviewing_content → text_complete
  //            → generating_image → image_complete → done
  // 5. 持久化到 Vercel KV: career:{slug}
  // 6. 写入审计日志
}
```

**✅ 验收**:
- `curl -N -X POST http://localhost:3000/api/ai-generate -H "Content-Type: application/json" -d '{"query":"碳中和管理师"}'` → 看到 7 种 SSE 事件
- Vercel KV 中存在生成的职业数据

---

### 步骤 18: 🔴 前端 AI 生成面板 + 反馈功能

**🎯 目标**: 实现前端 AI 生成交互界面

**📁 文件清单**:
```
└── src/components/search/
    ├── AIGeneratePanel.tsx     (修改，从占位变为完整实现)
    └── AIDisclaimer.tsx        (新建)
```

**🔧 操作指令**:

1. **`AIGeneratePanel.tsx`**:
   ```typescript
   'use client'
   // 状态机: idle → searching → generating_text → reviewing → generating_image → done → error
   // 使用 EventSource 或 fetch + ReadableStream 解析 SSE
   // 每收到事件更新 UI 状态
   // 完成后渲染完整职业卡片
   ```

2. **`AIDisclaimer.tsx`**:
   ```typescript
   // ⚠️ 此内容由 AI 生成，仅供参考
   // 仅 source === 'ai-generated' 时显示
   ```

3. 更新 `src/app/api/feedback/route.ts`:
   ```typescript
   // POST /api/feedback
   // 校验 + 去重 + 存储
   ```

**✅ 验收**:
- 搜索未命中 → 点击 AI 生成 → 进度正常显示 → 完成展示结果
- 审核失败时显示友好错误
- AI 内容页有免责声明
- 👍👎 反馈功能正常
- 重复投票被拦截

---

## 第五阶段：运营 + 完善（第 14 天+）

---

### 步骤 19: 🔴 速率限制 + 安全加固

**🎯 目标**: 防止 API 滥用

**📁 文件清单**:
```
└── src/lib/security.ts        (新建)
```

**🔧 操作指令**:

```bash
npm install @upstash/ratelimit @upstash/redis
```

```typescript
// src/lib/security.ts
import { Ratelimit } from '@upstash/ratelimit'
import { Redis } from '@upstash/redis'

// AI 生成: 3次/天/人
// 反馈: 10次/小时/人
// 搜索: 20次/秒/IP
```

修改 `next.config.ts` 添加 CSP headers。

**✅ 验收**:
- 第 4 次 AI 生成请求返回 429
- CSP headers 存在

---

### 步骤 20: 🟡 剩余 25 个职业 + 内容审核后台

**🎯 目标**: 职业数量达到 50+

**🔧 操作指令**:

- 继续编写 25 个职业的 Markdown 文件
- 每个职业配套概念图

**内容审核后台** (最小可行版本):

```typescript
// src/app/admin/review/page.tsx
// 密码保护
// 读取 Vercel KV 中的 pending-review 列表
// 查看 + 标记已审核
```

---

### 步骤 21: 🟡 暗色模式完善

**🎯 目标**: 暗色模式切换动画 + 持久化

**📁 文件清单**:
```
└── src/components/shared/
    └── ThemeToggle.tsx         (新建)
```

**✅ 验收**: 切换流畅、刷新保持、视觉一致

---

### 步骤 22: 🟡 数据埋点

**🎯 目标**: 接入分析

**🔧 操作指令**:
```bash
npm install @vercel/analytics
```

在 `layout.tsx` 中加入 `<Analytics />`，关键事件用 `track()`。

---

### 步骤 23: 🟡 测试补充

**🎯 目标**: 补充组件测试 + E2E

**🔧 操作指令**:
```bash
npm install -D vitest @testing-library/react @testing-library/jest-dom @playwright/test
```

按 `测试用例.md` 中的用例逐一实现。

---

### 步骤 24: 🟢 SEO 优化 + 后续

- Open Graph meta tags
- Schema.org JobPosting 结构化数据
- sitemap.xml 自动生成
- 国际化（可选）

---

## 快速参考

### 常用命令

```bash
npm run dev              # 启动开发服务器
npm run build            # 生产构建
npm test                 # 运行全部测试
npx tsx scripts/validate-content.ts  # 校验内容
npx tsx scripts/generate-images.ts   # 批量生成概念图
git add . && git commit -m "feat: xxx" && git push
```

### 关键文件索引

| 需求 | 文件 |
|------|------|
| 改配色 | `src/styles/globals.css` |
| 改分类 | `content/categories/tsinghua-categories.yaml` |
| 添加职业 | `content/careers/xxx.md` + 新图片 |
| 改搜索 | `src/lib/search.ts` |
| 改 AI Prompt | `src/lib/ai-generate.ts` |
| 改卡片样式 | `src/components/home/CareerCard.tsx` |
| 改详情页 | `src/components/career/Career*.tsx` |

---

> **执行建议**: 每个步骤完成后 git commit 一次。遇到问题时，先检查前一步的验收条件是否全部满足。
> 本文档可与 `DEVELOP.md`（设计参考）和 `测试用例.md`（测试参考）配合使用。
